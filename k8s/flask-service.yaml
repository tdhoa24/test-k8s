apiVersion: v1
kind: Service
metadata:
  name: flask-hello-service
spec:
  type: NodePort
  selector:
    app: flask-hello
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080 
